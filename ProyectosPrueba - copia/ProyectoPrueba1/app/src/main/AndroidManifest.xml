<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectoprueba1quehago">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoPrueba1QueHago">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.Inicio"/>
        <activity android:name=".Activities.Registro"/>
        <activity android:name=".Activities.App"/>
        <activity android:name=".Activities.PaginaDistrito"/>
        <activity android:name=".Activities.PopUpActividadCultural"
            android:theme="@style/AppTeme.TemaPopUp"/>


    </application>

</manifest>